spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/demo'
    username: 'demo'
    password: 'demo'
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true
